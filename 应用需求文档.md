# 碳纳米管生产数据分析系统 — 应用需求文档

## 一、项目背景

碳纳米管（CNT）生产线（L3 线、11A 线）每日产生大量生产数据，包括反应时间、故障时间、空烧/降清时间、产量和产率等指标。当前数据以 Excel 形式存储，缺乏系统化的分析工具。

现有分析脚本（`analysis.py`）已实现核心统计和可视化功能。本需求文档在此基础之上，提出一个**完整软件系统的功能需求**，并补充当前尚未覆盖但具有业务价值的增强需求。

---

## 二、核心业务概念

| 术语 | 说明 |
|------|------|
| 反应周期 | 同一炉号同一天的汇总数据（合并白班/夜班），为最小分析单元 |
| 反应时间 | 一个周期内实际生产运行的小时数 |
| 空烧时间 | 停机清理或空烧耗时（源自"停机清理空烧"列） |
| 降清时间 | 当前与空烧时间同源，后续可按字段拆分独立统计 |
| 故障时间 | 设备故障导致的停机时间 |
| 产率 | 产量 ÷ 反应时间（kg/h） |
| 生产线 | L3（E/F/G/H/B 区段，约 92 个炉号）和 11A（约 55 个炉号） |

---

## 三、功能需求

### F1 — 数据导入与清洗

**F1.1 多源数据导入**
- 支持导入 .xlsx / .xls / .csv 格式的生产数据
- 自动识别工作表（Sheet），跳过空表或列缺失的 Sheet

**F1.2 数据清洗**
- 自动修复常见录入错误（`9..2` → `9.2`、多余空格、中文标点等）
- 自动转换 Excel 日期序列号为标准日期
- 过滤无效行（"总计"行、空炉号行）
- 缺失值智能填充（故障时间/空烧时间 NaN 填 0，产量/反应时间 NaN 保留）

**F1.3 数据结构标准化**
- 将不同生产线的数据统一为标准格式
- 添加辅助列：生产线标识、年月、周期序号、来源追溯信息

---

### F2 — 炉子级统计分析

**F2.1 反应周期明细**
- 列出每个炉子每次反应周期的：反应时间、空烧时间、降清时间、故障时间、产量、产率
- 支持追溯至原始数据行

**F2.2 月度炉子平均**
- 按月份 + 炉号计算：平均反应时间、平均空烧时间、平均降清时间、平均故障时间、平均产率
- 输出为独立 Excel Sheet

**F2.3 全区各月平均**
- 不区分炉号，按月份汇总全区平均指标

**F2.4 自选炉子分析**
- 支持按精确炉号或多前缀匹配选择分析范围
- 如：`E` → 所有 E 开头炉号；`E01,F01` → 精确选择

**F2.5 前后 20% 筛选**
- **按月分别筛选**（而非跨月混排）
- 对 5 个指标分别排序，筛选每月前 20% 和后 20% 的炉号
- 反应时间/空烧/降清/故障 → 越低越好（前 20% = 时间最短）
- 产率 → 越高越好（前 20% = 产率最高）
- 输出汇总表，含排名序号

---

### F3 — 每日汇总与趋势

**F3.1 每日汇总**
- 统计全区每日：总产量、总反应时间、总故障时间、总空烧时间、总降清时间
- 按生产线（L3、11A）单独汇总

**F3.2 每日趋势图**
- 双面板图：上方柱状图（产量），下方折线图（各项时间指标）
- 自动适配日期范围和刻度
- 支持中文标签

---

### F4 — 每月汇总与趋势

**F4.1 每月汇总**
- 同 F3 口径，按月份汇总全区和各生产线数据

**F4.2 每月趋势图**
- 同 F3 风格，X 轴为月份

---

### F5 — 输出与格式

- Excel 输出：冻结首行、表头加粗着色、自动列宽
- 图表输出：PNG 格式，150 DPI，中文字体适配
- 输出目录独立（`output/`）

---

### F6 — 交互方式

- **命令行参数模式**：支持 `--all`、`--daily`、`--monthly`、`--furnace`、`--input`、`--list-furnaces`
- **交互式菜单模式**：无参数运行时提供菜单选择
- **批量模式**：可通过管道或脚本调用

---

## 四、补充需求建议

以下需求当前尚未实现，但基于业务场景提出，建议纳入后续版本。

---

### S1 — 同比/环比分析

**场景**：管理者需要对比本月与上月、本月与去年同月的指标变化。

**建议实现**：
- 环比增长率：`(本月 - 上月) / 上月 × 100%`
- 同比变化：`(本月 - 去年同月) / 去年同月 × 100%`
- 输出对照表，异常变动（±20%以上）高亮标注

---

### S2 — 单炉子生命周期趋势图

**场景**：针对单个炉子，观察其产率和故障率随时间的退化趋势，用于预测维护。

**建议实现**：
- 按日期绘制单炉子的产率、反应时间、故障时间曲线
- 添加移动平均线和趋势线（线性回归）
- 标注故障频发区段

---

### S3 — 故障模式分析

**场景**：识别高频故障炉号和故障时间分布规律。

**建议实现**：
- 按炉号统计故障总时间 / 故障次数排名
- 按星期几 / 月初月末 / 班次 维度分析故障分布
- 故障热力图（横轴=日期，纵轴=炉号，颜色=故障时间）

---

### S4 — 产率正态分布与异常检测

**场景**：识别异常低产的反应周期，及时追溯原因。

**建议实现**：
- 全炉产率分布直方图 + 正态拟合曲线
- 按 3σ 原则标记异常值
- 列出异常周期的详细信息（日期、炉号、班组、各项时间）

---

### S5 — 自动报表生成

**场景**：每日/每周/每月定时生成生产报表，无需手动运行脚本。

**建议实现**：
- 支持 Windows 任务计划 / cron 定时执行
- 报表模板化：可配置需要展示的指标和图表
- 自动发送邮件（附 Excel + PNG）

---

### S6 — Web 仪表盘

**场景**：管理者、工程师多人同时查看实时生产数据，无需安装 Python 环境。

**建议实现**：
- 基于 Streamlit / Dash 构建 Web 界面
- 页面布局：
  - 首页：当日总览卡片（总产量、总故障时间、运行炉数）
  - 炉子页：单炉趋势、排名
  - 汇总页：每日/每月趋势图，支持日期范围筛选
  - 对比页：自选多个炉子并列对比

---

### S7 — 数据告警

**场景**：关键指标超标时主动通知相关人员。

**建议实现**：
- 可配置的告警阈值：
  - 单炉故障时间 > X 小时/天
  - 全区日产 < Y kg
  - 产率 < Z kg/h 的炉子持续 N 天
- 告警方式：控制台输出标识 + 可扩展邮件/钉钉/企微通知

---

### S8 — 配置化

**场景**：生产线的炉号、指标定义可能变化，需要灵活配置。

**建议实现**：
- 通过 YAML/JSON 配置文件定义：
  - 生产线与炉号映射
  - 指标列映射（如源表列名变更）
  - 告警阈值
  - 前后百分比（默认 20% 可改为其他如 15%）

---

### S9 — 数据持久化

**场景**：历史数据积累后的跨月/跨年对比和高级查询。

**建议实现**：
- 每次导入后将清洗后数据存入 SQLite 数据库
- 支持按日期范围、炉号、生产线等条件查询
- 累计统计：运行总时长、总产量、平均产率等

---

## 五、非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 10 万行以内数据，清洗 + 全部分析在 30 秒内完成 |
| 兼容性 | Windows 10/11，Python 3.10+ |
| 字体 | 自动适配系统中文字体（微软雅黑/宋体/黑体） |
| 容错性 | 脏数据自动清洗，不因个别异常值中断 |
| 可追溯 | 所有统计结果可追溯到原始 Excel 行号 |

---

## 六、当前实现与需求的对应关系

| 需求 | 当前状态 |
|------|----------|
| F1 数据导入与清洗 | 已实现 |
| F2 炉子级统计 | 已实现（含按月前后20%筛选） |
| F3 每日汇总与趋势 | 已实现 |
| F4 每月汇总与趋势 | 已实现 |
| F5 输出与格式 | 已实现（含格式化） |
| F6 交互方式 | 已实现（命令行 + 交互菜单） |
| S1 同比环比 | 未实现 |
| S2 单炉趋势图 | 未实现 |
| S3 故障模式分析 | 未实现 |
| S4 异常检测 | 未实现 |
| S5 自动报表 | 未实现 |
| S6 Web 仪表盘 | 未实现 |
| S7 数据告警 | 未实现 |
| S8 配置化 | 部分实现（SOURCE_COLUMNS 可配置） |
| S9 数据持久化 | 未实现 |
