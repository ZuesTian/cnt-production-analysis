# 项目改进计划

## 当前状态

| 模块 | 行数 | 职责 |
|------|------|------|
| `analysis.py` | 958 | 数据处理、统计、可视化、CLI |
| `gui_app.py` | 624 | tkinter 桌面界面 |
| `启动分析软件.bat` | 5 | Windows 一键启动 |
| `应用需求文档.md` | - | 需求规格说明 |

核心需求 F1-F6 已 **100% 实现**。本轮已完成 P0-P2、I1-I6 的主要编码工作；长期项 I7-I9 仍待单独规划。

## 本轮执行状态

| 编号 | 状态 | 说明 |
|------|------|------|
| P0 | 已完成 | 已添加 `requirements.txt`，包含运行和测试依赖 |
| P1 | 已完成 | 已合并重复过滤逻辑，统一为 `select_cycles` |
| P2 | 已完成 | `CYCLE_METRICS` 已被月度平均和排名逻辑复用 |
| I1 | 已完成 | 单炉每日趋势图新增 7 日均线、线性趋势线、异常点和故障阈值标记 |
| I2 | 已完成 | 已添加 `tests/test_analysis.py`，覆盖加载清洗、炉号匹配、异常和故障统计 |
| I3 | 已完成 | GUI 启动增加 Pillow 依赖检查和友好提示 |
| I4 | 已完成 | 已添加 `config.yaml`，外部化列映射、生产线规则、排名比例和阈值 |
| I5 | 已完成 | 已实现低产率异常检测和 `异常检测报告_*.xlsx` |
| I6 | 已完成 | 已实现故障排名、星期分布和故障热力图 |
| I7-I9 | 未开始 | Web 仪表盘、定时报表、SQLite 持久化留作后续阶段 |

---

## 一、立即修复（P0-P1）

### P0 — 添加 `requirements.txt`

当前缺少依赖声明文件，其他人/环境无法一键安装。

```text
# requirements.txt
pandas>=2.0
matplotlib>=3.7
numpy>=1.24
openpyxl>=3.1
Pillow>=10.0
```

### P1 — 合并重复函数

`filter_cycles` ([analysis.py:362](analysis.py#L362)) 和 `select_cycles` ([analysis.py:356](analysis.py#L356)) 功能相同，仅 print 信息不同。

**方案**：保留 `select_cycles`，将 print 逻辑提到调用方。改动仅影响 [`furnace_stats`](analysis.py#L494) 和 [`furnace_daily_trend_data`](analysis.py#L659) 两处调用。

```python
# 删除 filter_cycles，统一用 select_cycles
def select_cycles(cycles: pd.DataFrame, furnace_list: list[str] | None) -> pd.DataFrame:
    if furnace_list is None:
        return cycles.copy()
    filtered = cycles[cycles["炉号"].isin(furnace_list)].copy()
    if filtered.empty:
        raise ValueError("选中的炉号没有数据")
    return filtered
```

### P2 — 删除或引用冗余常量

`CYCLE_METRICS` ([analysis.py:52](analysis.py#L52)) 定义后未被引用。`monthly_furnace_average` 和 `region_monthly_average` 内部重复定义了相同的指标列表。

**方案**：将这些函数内的硬编码列表替换为引用 `CYCLE_METRICS`。

```python
# monthly_furnace_average 中：
# 改前：
metrics = ["反应时间", "空烧时间", "降清时间", "故障时间", "产率"]
# 改后：
metrics = CYCLE_METRICS
```

---

## 二、短期改进（1-2 周）

### I1 — 单炉趋势图增加趋势线（对应 S2）

当前 `plot_single_furnace_daily_trend` 只绘制原始数据点。加上 7 日移动平均和线性回归趋势线，用于判断炉子退化。

**改动位置**：[analysis.py:703](analysis.py#L703) `plot_single_furnace_daily_trend`

**方案**：
- 产率曲线叠加 7 日移动平均线（`df.rolling(7).mean()`）
- 产率加线性回归趋势线（`numpy.polyfit`），标注斜率
- 故障时间超过阈值的天数用红色竖线标记

### I2 — 添加基础测试

当前无任何测试。至少覆盖数据加载和关键统计函数。

**方案**：添加 `test_analysis.py`，使用项目自带数据做 smoke test。

```python
# test_analysis.py
import analysis

def test_load_data():
    cycles = analysis.load_and_clean_data(analysis.find_input_file())
    assert len(cycles) > 0
    assert "炉号" in cycles.columns
    assert cycles["产率"].notna().sum() > 0

def test_monthly_average():
    cycles = analysis.load_and_clean_data(analysis.find_input_file())
    avg = analysis.monthly_furnace_average(cycles)
    assert len(avg) > 0
    assert "平均产率" in avg.columns

def test_rank_top_bottom():
    cycles = analysis.load_and_clean_data(analysis.find_input_file())
    avg = analysis.monthly_furnace_average(cycles)
    result = analysis.rank_monthly_top_bottom(avg)
    assert "前后20汇总" in result
    assert len(result["前后20汇总"]) > 0
```

### I3 — GUI 增加依赖检查

当前 GUI 启动时如果缺少 Pillow 会直接崩溃。

**方案**：在 `gui_app.py` 入口加 try/except，给出友好提示。

```python
def main():
    try:
        from PIL import Image, ImageTk
    except ImportError:
        print("缺少 Pillow 库，请运行：pip install Pillow")
        import sys
        sys.exit(1)
    app = AnalysisApp()
    app.mainloop()
```

---

## 三、中期改进（2-4 周）

### I4 — 配置外部化（对应 S8）

将 `SOURCE_COLUMNS`、炉号分组、告警阈值等从代码中抽离到 YAML 配置文件。

**方案**：新增 `config.yaml`

```yaml
# config.yaml
source_columns:
  date: 日期
  team: 班组
  furnace: 炉号
  reaction_time: 生产时间
  fault_time: 设备故障影响时间
  clean_empty_burn_time: 停机清理空烧
  output: 产量
  source_yield: 小时产能

production_lines:
  L3:
    patterns: ["E", "F", "G", "H", "B"]
  "11A":
    patterns: ["11A"]

ranking:
  top_percent: 0.2

alert_thresholds:
  max_fault_hours_per_day: 24
  min_yield_rate: 50
```

`analysis.py` 启动时读取配置，命令行参数可覆盖。

### I5 — 异常检测模块（对应 S4）

新增 `detect_anomalies()` 函数，基于 3σ 原则识别异常低产周期。

**方案**：
- 按炉号分别计算产率均值和标准差
- 标记低于 `μ - 2σ` 和 `μ - 3σ` 的记录
- 输出异常周期列表（日期、炉号、班组、产率、偏差倍数）
- 在单炉趋势图上用红色标记异常点

```python
def detect_anomalies(cycles: pd.DataFrame, sigma: float = 2.0) -> pd.DataFrame:
    """标记每个炉子的产率异常周期"""
    results = []
    for furnace, sub in cycles.groupby("炉号"):
        mean = sub["产率"].mean()
        std = sub["产率"].std()
        if pd.isna(std) or std == 0:
            continue
        threshold = mean - sigma * std
        anomalies = sub[sub["产率"] < threshold].copy()
        anomalies["产率均值"] = round(mean, 2)
        anomalies["产率标准差"] = round(std, 2)
        anomalies["偏差σ"] = ((mean - anomalies["产率"]) / std).round(1)
        results.append(anomalies)
    return pd.concat(results, ignore_index=True) if results else pd.DataFrame()
```

### I6 — 故障模式分析（对应 S3）

新增故障热力图和故障排名统计。

**方案**：
- **故障排名表**：按炉号统计总故障时间、故障次数、故障天数，降序排列
- **故障热力图**：横轴=日期，纵轴=炉号，颜色=故障时间。一眼看出哪些炉子/日期故障集中
- **时间分布**：按小时段（0-6, 6-12, 12-18, 18-24）统计故障分布

---

## 四、长期改进（1-3 月）

### I7 — Web 仪表盘（对应 S6）

使用 Streamlit 构建 Web 界面，支持多人同时访问。

**页面规划**：
| 页面 | 内容 |
|------|------|
| 首页 | 当日总览卡片（产量/故障/运行炉数），近 7 天趋势小图 |
| 炉子分析 | 单炉趋势、炉子对比、排名表 |
| 汇总分析 | 每日/每月趋势，日期范围选择器 |
| 异常报告 | 异常周期列表、故障热力图 |
| 数据管理 | 上传新数据、历史数据查询 |

### I8 — 定时报表（对应 S5）

**方案**：
- Windows：用"任务计划程序"定时执行 `python analysis.py --all`
- 报表生成后通过 SMTP 发送邮件（附件 Excel + PNG）
- 配置文件指定收件人和频率

### I9 — 数据持久化（对应 S9）

**方案**：
- SQLite 数据库存储清洗后的 cycles 表
- 每次导入新文件时增量追加（按日期+炉号去重）
- 支持历史数据查询和跨月对比

---

## 优先级总览

| 优先级 | 编号 | 项目 | 预估工时 |
|--------|------|------|----------|
| 🔴 P0 | P0 | requirements.txt | 0.1h |
| 🔴 P0 | P1 | 合并重复函数 | 0.3h |
| 🔴 P0 | P2 | 引用 CYCLE_METRICS | 0.2h |
| 🟡 P1 | I1 | 单炉趋势线 | 2h |
| 🟡 P1 | I2 | 基础测试 | 2h |
| 🟡 P1 | I3 | GUI 依赖检查 | 0.3h |
| 🟢 P2 | I4 | 配置外部化 | 4h |
| 🟢 P2 | I5 | 异常检测 | 3h |
| 🟢 P2 | I6 | 故障模式分析 | 4h |
| 🔵 P3 | I7 | Web 仪表盘 | 20h |
| 🔵 P3 | I8 | 定时报表 | 6h |
| 🔵 P3 | I9 | 数据持久化 | 6h |

---

## 改进后项目结构

```
碳纳米管生产数据分析/
├── analysis.py              # 分析引擎
├── gui_app.py               # 桌面 GUI
├── test_analysis.py         # 单元测试     ← 新增
├── config.yaml              # 配置文件     ← 新增
├── requirements.txt         # 依赖声明     ← 新增
├── 启动分析软件.bat
├── 应用需求文档.md
├── 改进计划.md              # 本文件       ← 新增
└── output/
```
